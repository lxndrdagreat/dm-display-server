version: '3.7'

services:
  api:
    container_name: 'dm-display-api'
    build:
      context: '.'
    ports:
      - "${SERVER_PUBLIC_PORT}:${EXPRESS_SERVER_PORT}"
    volumes:
      - .env:/home/node/app/.env
